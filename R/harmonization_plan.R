# Harmonization plan: standardize data into a common format.

harmonization_plan <- list(

    #   tar_target(
#     name = harmonized_sites,
#     command = harmonize_community_sites(community_download)
#   ),

#   tar_target(
#     name = harmonized_taxonomy,
#     command = harmonize_taxonomy(harmonized_sites)
#   )

)
