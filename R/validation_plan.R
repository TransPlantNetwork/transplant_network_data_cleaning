# Validation plan: checks and QA on cleaned / harmonized data.


validation_plan <- list(

#   tar_target(
#     name = validation_report,
#     command = validate_harmonized_data(harmonized_data)
#   )

)
